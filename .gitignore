#
# Don't add editor/environment-specific files. Put those in your global .gitignore file.
# https://gist.github.com/subfuzion/db7f57fff2fb6998a16c
#

### direnv ###
.direnv
# NOTE: We intentionally track a template `.envrc`; this prevents new local ones from being committed.
.envrc

# Logs
logs
*.log

# dotenv environment variable files
# NOTE: We intentionally track a template `.env`; this prevents new local ones from being committed.
.env
.env.*.local
.env.local

# copier update rejection files
# So, this is ridiculous ... but copier's entire "update" capablity relies on these files, even
# while their documentation warns against their presence and urges you to not commit them.
# But if you .gitignore them, the whole update workflow breaks.
# facepalm.
# *.rej

### Rust ###
# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# Generated man pages and shell completions (keep .gitkeep)
dist/share/man/man1/*.1
dist/share/completions/*
!dist/share/**/.gitkeep

# Benchmark results (keep .gitkeep)
bench-reports/*
!bench-reports/.gitkeep

# site
site/node_modules/

# agent handoffs/pickups
#.handoffs/

# commit message draft
.commit.md
commit.txt

# Private memory (never committed)
PRIVATE_MEMORY.md

# scratch script and scratch directory
# for little tests, scripts or notes
scratch
scratch/
